export class Message {
    constructor(
        public name: string,
        public phome: number,
        public email: string,
        public message: string,
        public published: boolean
    ) { }
}
